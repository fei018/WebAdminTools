var ValStartDate = function () {
    var ob = document.getElementsByClassName("field-validation-valid");
    for (let i of ob) {
        var att = i.getAttribute("data-valmsg-for");
        if (att != null) {
            
        }
    }
}